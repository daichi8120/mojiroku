# Cloudflare Pages リダイレクト。
# 「公開URL」と「ファイルの置き場所」を分離する: ユーザーには常に mojiroku.com/download を見せ、
# 実体（GitHub Releases）を隠す。将来 R2 等へ移しても、ここの行先を1箇所変えるだけで済む。
#
# 注意: GitHub の releases/latest/download/... は draft でも prerelease でもない「公開リリース」にしか解決しない。
# 初回は必ずフル公開リリースで出すこと（beta 表記で出すなら latest でなく固定タグへ向ける）。
# アセット名はバージョン非依存の安定名（mojiroku-macos-aarch64.dmg）を使い、版上げでリンクを壊さない。
/download  https://github.com/daichi8120/mojiroku-releases/releases/latest/download/mojiroku-macos-aarch64.dmg  302
