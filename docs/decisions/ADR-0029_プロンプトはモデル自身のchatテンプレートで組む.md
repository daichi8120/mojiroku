# ADR-0029: プロンプトはモデル自身の chat テンプレートで組む

- 状態: 採用
- 日付: 2026-08-25
- 関連: [ADR-0007](./ADR-0007_要約llamaを別バイナリsidecarに分離.md)（sidecar 分離）、Issue #4（モデル横断評価）、Issue #30（スペック連動のモデル選択）

## Context

要約 sidecar（`crates/mojiroku-llm`）は、Qwen2.5 の ChatML を**ソースに直書き**していた。

```
<|im_start|>system\n…<|im_end|>\n<|im_start|>user\n{本文}<|im_end|>\n<|im_start|>assistant\n
```

既定モデルが Qwen 系である限りこれで正しく、実際に正しく動いていた。問題は**別のモデルを試そうとしたとき**に出た。

### 実測で分かったこと

Issue #4 のモデル横断評価（11 モデル × 4 タスク = 154 生成）で、制御トークンが出力へ漏れる現象が出た。**系統でぴったり分かれた。**

| 系統 | 制御トークンの漏れ |
|---|---|
| Qwen 系 6 本 | **0/14 すべて** |
| 非 Qwen 系 5 本 | gemma-12b 9/14・Phi 9/14・sarashina 8/14・gemma-4b 6/14・Llama 2/14 |

原因は単純だった。gemma・Llama・Phi にとって `<|im_end|>` は**特殊トークンではない**。ChatML を食わされたモデルがそれを真似て出力すると、ただの文字列としてトークン化されるので `is_eog_token` に一度も引っかからず、生成が止まらない。

**つまり評価の半分は「モデルの質」ではなく「食わせ方」を測っていた。**

この状態では、日本語に強いモデルへの差し替え（Issue #4）も、端末スペックに応じた段の切り替え（Issue #30）も検討できない。**モデル選択の自由を、配管が奪っていた。**

## Decision

**プロンプトはモデル自身の chat テンプレートで組む。**

GGUF はメタデータに `tokenizer.chat_template` を持つ。`llama_chat_apply_template` でそれを適用する。

- トークナイズは `parse_special = true` なので、テンプレート由来の特殊トークンは文字列ではなく特殊トークンとして解釈される
- 停止判定は従来どおり `is_eog_token`。テンプレートが正しければモデル固有の終端で正しく止まる

### 付随して決めたこと

| 論点 | 決定 | 理由 |
|---|---|---|
| system ロール非対応のモデル | system を user の冒頭へ**畳んで**再試行する | gemma 系は system を受け付けない。捨てると指示の言語が失われる |
| テンプレートを持たないモデル | 従来の ChatML へフォールバック | GGUF にテンプレートが入っていない古い変換への保険 |
| BOS | **決め打ちしない。** 先に BOS 無しで引き、先頭が BOS でなければモデルの設定に従って付け直す | テンプレートが BOS を文字列で出すもの（Qwen 系）と、出さずにトークナイズ側に任せるもの（gemma 系）が混在する |
| 本文の切り詰め | テンプレート**適用前**に行う。オーバーヘッドは空の本文で一度組んで実測する | テンプレートの長さはモデルごとに違う。決め打ちの prefix/suffix 長という前提はもう無い |
| 思考を切る（`--no-think`） | テンプレート適用後に空の think ブロックを足す形で維持 | Qwen3 系は既定で思考を吐く。テンプレートが正しくても長さの問題は残る |

## 検証

### 現行モデルに退行なし

Qwen2.5-7B で、旧バイナリと新バイナリに同じ録音 5 本を流し、**出力が完全一致**することを確認した。

GGUF に焼かれた Qwen2.5 のテンプレートは、手で書いていた ChatML と同一だった。つまりこの変更は、Qwen 系にとっては何も変えていない。

### 非 Qwen 系が動くようになった

| モデル | 変更前 | 変更後 |
|---|---|---|
| gemma-3-4b | `---` しか返さない | 通常の応答 |
| Llama-3.2-3B | 制御トークン漏れ | 漏れなし |
| Phi-4-mini | 制御トークン漏れ | 漏れなし |
| sarashina2.2-3b | 制御トークン漏れ | 漏れなし |

gemma が `---` しか返さなかったのは BOS 欠落によるもので、**テンプレート自体は正しく当たっていた**ため原因が見えにくかった。`MOJIROKU_LLM_DEBUG=1` で組み上がったプロンプトを stderr に出せるようにしたのは、この種の切り分けのため。

## 影響・制約

- **モデルの質の比較は、この変更の後にやり直す必要がある。** 変更前の結果は「Qwen の食わせ方を強制したときの成績」であって、モデルの実力ではない
- テンプレートは llama.cpp のテンプレートエンジン（Jinja の部分実装）が解釈する。複雑なテンプレートで解釈が食い違う可能性は残る。`MOJIROKU_LLM_DEBUG=1` で目視できるようにしてある
- 出力がモデルごとに変わるということは、**プロンプトもモデルごとに詰め直す必要がある**。現行のプロンプトは Qwen2.5 に合わせて実測で詰めたもの（Issue #4）
- `--no-think` の空 think ブロックは Qwen3 系の作法。他系統に渡しても無害だが、意味も無い

## 対象外

- どのモデルを既定にするか。これは Issue #4 の評価と Issue #30 の段設計で決める
- BYOK（クラウド）経路。chat テンプレートはローカル推論の話
- 文字起こし（whisper）と話者分離（sherpa-onnx）。別の推論スタック
